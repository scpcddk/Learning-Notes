# Python核心语法(对比化学习)(凡未标注内容皆为差别微小)

## **一、入门基础**

### **(一).运行原理**

#### 1.运行方式
+ 交互式（REPL）：终端输入 `python`，逐行执行，即时反馈
+ 脚本式：编写 `.py` 文件，执行 `python filename.py`

#### 2.执行原理
+ 源码(.py) → 字节码(.pyc) → Python虚拟机(PVM) → 运行结果
+ Python 是==解释型语言==（严格说是"先编译后解释"）
+ 字节码是平台无关的中间代码

---

### **(二).动态类型特性**（变量即标签，类型随赋值改变）

+ **变量是标签，不是盒子**
+ **变量本身没有类型，对象才有类型**,无需声明类型，运行时动态确定
+ 赋值只是让变量"指向"对象,同一变量可先后指向**不同类型**的对象
+ 多个变量可以指向同一个对象：a = b = [1, 2, 3]

```python
a = 10      # a 标签贴在 10 这个对象上
a = "hello" # a 标签撕下来，贴到 "hello" 对象上
```

---

### **(三).一切皆对象**

#### 1.什么是"对象"
+ 对象 = 数据（属性）+ 行为（方法）
+ 对象存储在堆内存中，由垃圾回收器管理

#### 2.所有 Python 元素都是对象
+ 可以动态给对象添加属性
+ 可以将函数作为参数传递（高阶函数）
+ 所有类型都有统一的类型体系

---

## **二、数据存储与运算**

### ==**(一).字面量与变量**==

#### **1.字面量与c++对比**

* 字面量是直接书写的固定值
* type()语句可得到数据类型，`type(要查看的数据类型)`
* isinstance()检查是否属于指定的类型，返回bool值，`isinstance(数据，类型)`

| 类别 | Python | C++ | 说明 |
|------|--------|-----|------|
| 整数精度 | **无限精度，无溢出** | 受类型范围限制 | Python可计算超大整数 |
| 数字分隔符 | 支持 `1_000_000` | 不支持 | 提高可读性 |
| 布尔值 | `True` / `False`（首字母大写） | `true` / `false` | Python中布尔是int子类 |
| 布尔运算 | `True + True == 2` | 不允许 | **Python布尔可参与算术** |
| 多行字符串 | **三引号** `"""..."""` | 需用 `\n` 或宏 | Python原生支持 |
| 原始字符串 | `r"C:\path"` | 无 | **原始字符串(r)**转义字符失效，按原样输出 |
| 复数 | `3 + 4j` | 无内置 | Python原生支持复数 |
| 空值 | `None`（对象） | `nullptr` / `NULL`（指针） | None是单例，用 `is` 比较 |
| 类型后缀 | 不需要 | `100L` / `3.14f` | Python自动适配 |

#### **2.变量与c++对比**

* 变量是用来存储单个数据的容器
* 一条语句可以定义多个变量，也可以连续赋值(`a,b = 1,"Python"`)

| 对比项 | Python | C++ |
|--------|--------|-----|
| 变量本质 | 标签（贴在对象上） | 内存容器（存储值） |
| 类型绑定 | 变量无类型，对象有类型 | 变量有固定类型 |
| 类型改变 | 允许，随时可指向不同类型 | 不允许 |
| 声明要求 | 不需要，直接赋值即创建 | 必须声明类型 |
| 未初始化 | **不存在（赋值才有变量）** | 存在（值不确定） |
| 指针/引用 | **所有变量都是引用语义** | 分值语义、指针、引用 |
| 内存管理 | 自动垃圾回收 | 手动 new/delete |

---

### **(二).算术运算符差异**

| 对比项 | Python | C++ |
|--------|--------|-----|
| 除法 `/` | 结果总是 `float` | 整数除法结果截断 |
| 整除 `//` | 向下取整 | 无此运算符 |
| 取模 `%` | 结果符号与**除数**相同 | 结果符号与**被除数**相同 |
| 幂运算 `**` | ✅ 支持 | 无，用 `pow()` |

---

### **(三).逻辑运算符短路运算（and / or / not）**

#### 1. 逻辑运算符差异

| 对比项 | Python | C++ |
|--------|--------|-----|
| 与 | `and` | `&&` |
| 或 | `or` | `\|\|` |
| **非** | `not` | `!` |
| 返回值 | 返回操作数本身（非布尔） | 返回布尔值（`true`/`false`） |
| 短路运算 | ✅ 支持 | ✅ 支持 |

#### 2.返回值对比示例

+ `and` 规则：返回第一个假值，否则返回最后一个值
+ or 规则：返回第一个真值，否则返回最后一个值

| 表达式 | Python | C++ |
|--------|--------|-----|
| `10 and 20` | `20` | `true`(1) |
| `0 and 20` | `0` | `false`(0) |
| `10 or 20` | `10` | `true`(1) |
| `0 or 20` | `20` | `true`(1) |

---

### **(四).连续比较（`a < b < c`）**


| 对比项 | Python | C++ |
|--------|--------|-----|
| **连续比较** | `a < b < c` ✅ 支持 | 不支持，需写 `a < b && b < c` |

---

### **(五).赋值运算符（含链式赋值、增量赋值）**

| 对比项 | Python | C++ |
|--------|--------|-----|
| 基本赋值 | `=` | `=` |
| 增量赋值 | `+=` `-=` `*=` `/=` `//=` `%=` `**=` | `+=` `-=` `*=` `/=` `%=` `<<=` `>>=` `&=` `\|=` `^=` |
| 链式赋值 | `a = b = 10` ✅ | `a = b = 10` ✅ |
| 赋值表达式返回值 | 无（赋值不是表达式） | 有（返回赋值后的值） |
| `if a = 10` | ❌ 语法错误**(赋值和比较明确区分)** | ✅ **允许（赋值作为表达式）** |

---

### **(六).成员与身份运算符(Python 独有)**

| 运算符 | 作用 | 示例 | 返回值 |
|--------|------|------|-------|
| `in` / `not in` | 判断元素是否在容器中 | `"a" in "abc"` | `True` 或 `False` |
| `is` / `is not` | 判断是否为同一对象（比较 id） | `a is b` | `True` 或 `False` |

---

## **三、字符串**

- 定义方式（单引号/双引号/三引号，原始字符串 r""）
- 转义字符
- 格式化（f-string 优先）
- 常用方法（split / join / strip / replace / find / upper / lower / startswith / isdigit）

---

## **四、输入输出**

### **(一).Python vs C++ 代码对比表**

| 对比项 | Python | C++ |
|--------|--------|-----|
| **打印分隔线** | `print("=" * 50)` | `cout << string(50, '=') << endl;` |
| **打印标题** | `print("Python 占位符示例")` | `cout << "C++ 占位符示例" << endl;` |
| **定义变量** | `name, age, score = "张三", 20, 95.5` | `string name = "张三";`<br>`int age = 20;`<br>`double score = 95.5;` |
| ==**f-string / cout**== | `print(f"姓名:{name}, 年龄:{age}, 成绩:{score}")` | `cout << "姓名:" << name << ", 年龄:" << age << ", 成绩:" << score << endl;` |
| **format / printf** | `print("姓名:{}, 年龄:{}, 成绩:{}".format(name, age, score))` | `printf("姓名:%s, 年龄:%d, 成绩:%.1f\n", name.c_str(), age, score);` |
| ==**旧式% / printf**== | `print("姓名:%s, 年龄:%d, 成绩:%.1f" % (name, age, score))` | `printf("姓名:%s, 年龄:%d, 成绩:%.1f\n", name.c_str(), age, score);` |
| **格式控制** | `print(f"成绩:{score:.1f}, 编号:{age:04d}")` | `printf("成绩:%.1f, 编号:%04d\n", score, age);` |

---

### **(二).print() 的 sep / end 参数**

---

### **(三).input() 输入（始终返回字符串）**

#### 1.Python vs C++ 差异

| 对比项 | Python | C++ |
|--------|--------|-----|
| 输入函数 | `input()` | `cin` / `getline()` |
| 返回值类型 | **始终返回字符串** | 根据变量类型读取 |
| 类型转换 | 需要手动转换 | 自动（`cin >> int_var`） |
| 提示信息 | 内置参数 `input("提示语")` | 需单独 `cout` 输出 |
| 空格处理 | 默认读取整行 | `cin >>` 以空格分隔，`getline()` 读整行 |

#### 2.**代码**

```python
# Python
name = input("请输入姓名：")      # 始终返回 str
age = input("请输入年龄：")        # 年龄也是 str
age_int = int(age)                # 必须手动转换

# 链式写法
age = int(input("请输入年龄："))
```

---

## **五、流程控制**

### **(一).缩进规则（无大括号）**

#### 1.缩进（Python vs C++ 核心差异）

| 对比项 | Python | C++ |
|--------|--------|-----|
| 代码块标记 | **缩进**（强制） | 大括号 `{}`（缩进仅用于可读性） |
| 缩进量 | 统一（通常4空格） | 无要求 |
| 混合缩进 | ❌ 禁止（Tab/空格混用报错） | ✅ 允许 |
| 代码块结束 | 缩进减少即结束 | 右大括号 `}` |

#### 2. 核心原理

**Python：缩进即语法**
+ 同一代码块必须缩进一致
+ 嵌套层级增加缩进
+ 建议使用4空格（PEP 8标准）

```python
# 缩进决定代码归属
if True:
    print("属于if")      # 4空格缩进
    print("也属于if")
print("不属于if")         # 缩进恢复，if块结束
```

---

### **(二).if / elif / else**

| 对比项 | Python | C++ |
|--------|--------|-----|
| `if` 写法 | `if 条件:` | `if (条件)` |
| **`else if` 写法** | `elif 条件:` | `else if (条件)` |
| `else` 写法 | `else:` | `else` |
| 代码块标记 | **缩进 + 冒号 `:`** | 大括号 `{}` |
| **条件括号** | 不需要 | 必须加 `()` |
| 条件类型 | 任意类型（自动转布尔） | 布尔表达式 |
| 赋值作条件 | ❌ 语法错误 | ✅ 允许 |

---

### **(三).match-case**（模式匹配，Python 3.10+）

| 对比项 | Python `match-case` | C++ `switch` |
|--------|---------------------|--------------|
| 语法 | `match 值:`<br>`    case 模式:`<br>`        代码`<br>`    case _:`<br>`        默认代码` | `switch (表达式) {`<br>`    case 常量:`<br>`        代码; break;`<br>`    default:`<br>`        默认代码; break;`<br>`}` |
| 匹配值类型 | **任意类型** | 整型、枚举、字符 |
| 字符串匹配 | ✅ 支持 | ❌ 不支持 |
| 类型匹配 | ✅ 支持 | ❌ 不支持 |
| 解包匹配 | ✅ 支持（列表、元组） | ❌ 不支持 |
| 条件守卫（guard） | ✅ 支持 | ❌ 不支持 |
| **多个值匹配** | `\|` 分隔 | 连续写多个 `case` |
| `break` 需要 | ❌ **不需要（不会贯穿）** | ✅ 需要 |
| 默认分支 | `case _:`(通配符，匹配任意值) | `default:` |

* `case x:` 会将匹配到的值**绑定到变量 `x`**，后续代码可以使用该变量。

```python
value = 42
# 使用 case _（不绑定）
match value:
    case 1:
        print("匹配到1")
    case _:
        print("匹配到其他值")   # 无法知道具体是什么值

# 使用 case x（变量绑定）    变量名可以任意取
match value:
    case 1:
        print("匹配到1")
    case other:
        print(f"匹配到: {other}")   # 可以使用 other，输出 42
```

---

### **(四).for 循环**（遍历可迭代对象）

| 对比项 | Python | C++ |
|--------|--------|-----|
| 语法 | `for 变量 in 可迭代对象:` | `for (初始化; 条件; 增量)` |
| 循环对象 | 遍历可迭代对象（列表、字符串、range等） | 计数器控制 |
| `else` 子句 | ✅ 支持 | ❌ 不支持 |
| 修改循环变量 | 不影响下一次迭代 | 影响 |

```python
for 变量 in 可迭代对象:
    循环体代码
else:
    循环正常结束（无 break）时执行
```

---

### **(五).range() 函数**

#### 1.对比

| 对比项 | Python `range()` | C++ 循环控制 |
|--------|------------------|--------------|
| 类型 | 内置函数，返回可迭代对象 | 无直接对应 |
| 内存 | 惰性求值（不存储所有值） | 需要变量存储 |
| 语法 | `range(stop)` 或 `range(start, stop[, step])` | 手动初始化、条件、增量 |

#### 2.**语法**

```python
range(stop)                # 0 到 stop-1，不包含stop本身
range(start, stop)         # start 到 stop-1
range(start, stop, step)   # start 到 stop-1，步长为 step
```

---

### **(六).while 循环**

| 对比项 | Python | C++ |
|--------|--------|-----|
| 语法 | `while 条件:` | `while (条件)` |
| 代码块标记 | 缩进 + 冒号 `:` | 大括号 `{}` |
| 条件括号 | 不需要 | 必须加 `()` |
| 条件类型 | 任意类型（自动转布尔） | 布尔表达式 |
| **`else` 子句** | ✅ 支持 | ❌ 不支持 |

---

## **六、数据容器**

### **(一).列表（可变，切片，解包，推导式，append/pop/sort/reverse）**

---

### **(二).元组（不可变，组包解包，多返回值的本质）**

---

### **(三).集合（无序去重，并集/交集/差集）**

---

### **(四).字典（键值对，键必须不可变，keys/values/items/get/pop/update）**

---

### **(五).容器对比与选择**

---

### **(六).深拷贝与浅拷贝（copy 模块）**

---

### **(七).collections（defaultdict / Counter / deque）**

---

## **七、函数**
- 定义与调用（def）
- 参数（位置 / 关键字 / 默认 / *args / **kwargs）
- 多返回值（自动打包为元组）
- 作用域（global / nonlocal）
- 文档字符串（docstring）
- 递归
- lambda 表达式
- 高阶函数（函数作为参数）
- 闭包
- 装饰器
- 生成器（yield）

---

## **八、类型注解**
- 参数与返回值注解
- typing 模块（List / Dict / Optional / Union）
- 类型别名

---

## **九、模块与包**
- import / from / as
- `__name__ == "__main__"`
- 包与 `__init__.py`
- 绝对导入与相对导入
- 虚拟环境（venv）
- pip

---

## **十、文件与异常**
- open() 与文件模式
- with 上下文管理器
- 异常处理（try / except / else / finally）
- raise 与自定义异常

---

## **十一、面向对象**
- 类与对象
- `__init__` 构造方法
- 实例属性与类属性
- 实例方法 / 类方法（@classmethod）/ 静态方法（@staticmethod）
- `self` 显式传递
- 属性私有化（`_` / `__` 命名约定）
- @property（替代 getter/setter）
- 魔法方法（`__str__` / `__repr__` / `__len__` / `__add__` 等）
- 继承（单继承 / 多继承 / super / MRO）
- 方法重写
- 抽象类（abc 模块）

---

## **十二、常用标准库**
- os / pathlib
- sys.argv
- datetime / time
- json
- re
- logging
- argparse

---

## **十三、并发（按需）**
- threading
- multiprocessing
- asyncio

---

## **十四、工程化**
- PEP 8
- mypy 类型检查
- unittest / pytest

---