# Python 基础练习题（10道）

---

## 第1题：变量交换

```python
# 给定两个变量
a = 10
b = 20

# 请交换 a 和 b 的值（使用 Python 方式）
# 你的代码：

print(a, b)  # 应输出 20 10
```

---

## 第2题：字符串格式化

```python
name = "张三"
score = 95.5

# 使用 f-string 输出： "张三的成绩是95.5分"
# 你的代码：

```

---

## 第3题：判断奇偶数

```python
num = 7

# 判断 num 是奇数还是偶数，输出 "奇数" 或 "偶数"
# 你的代码：

```

---

## 第4题：列表操作

```python
lst = [1, 2, 3, 4, 5]

# 1. 在列表末尾添加数字 6
# 2. 在索引 2 的位置插入数字 10
# 3. 删除列表中的数字 4
# 你的代码：

print(lst)  # 应输出 [1, 2, 10, 3, 5, 6]
```

---

## 第5题：列表推导式

```python
# 使用列表推导式生成 1 到 10 中所有偶数的平方
# 你的代码：

print(result)  # 应输出 [4, 16, 36, 64, 100]
```

---

## 第6题：元组解包

```python
point = (5, 10)

# 将元组解包为变量 x 和 y，然后输出 "x=5, y=10"
# 你的代码：

```

---

## 第7题：字符串方法

```python
s = "  Hello World  "

# 1. 去除首尾空格
# 2. 转换为全小写
# 3. 将 "world" 替换为 "Python"
# 你的代码：

print(s)  # 应输出 "hello python"
```

---

## 第8题：集合去重

```python
lst = [1, 2, 2, 3, 4, 4, 5, 1]

# 使用集合去除重复元素，并保持原顺序
# 你的代码：

print(unique)  # 应输出 [1, 2, 3, 4, 5]
```

---

## 第9题：循环与条件

```python
# 打印 1 到 50 之间所有能被 3 整除但不能被 5 整除的数字
# 你的代码：

```

---

## 第10题：综合应用题

```python
# 需求：用户输入一串数字，用空格分隔
# 例如输入："10 20 30 40 50"
# 要求：
# 1. 将输入转换为整数列表
# 2. 计算列表的总和
# 3. 输出最大值和最小值

user_input = input("请输入数字，用空格分隔：")
# 你的代码：

```

---

# 参考答案

<details>
<summary>点击查看答案</summary>

## 第1题
```python
a, b = b, a
```

## 第2题
```python
print(f"{name}的成绩是{score}分")
```

## 第3题
```python
## if num % 2 == 0:
print("偶数")
## else:
print("奇数")
```

## 第4题
```python
lst.append(6)
lst.insert(2, 10)
lst.remove(4)
```

## 第5题
```python
result = [x**2 for x in range(1, 11) if x % 2 == 0]
```

## 第6题
```python
x, y = point
print(f"x={x}, y={y}")
```

## 第7题
```python
s = s.strip()
s = s.lower()
s = s.replace("world", "python")
```

## 第8题
```python
unique = list(dict.fromkeys(lst))
```

## 第9题
```python
## for i in range(1, 51):
## if i % 3 == 0 and i % 5 != 0:
print(i)
```

## 第10题
```python
nums = [int(x) for x in user_input.split()]
print(f"总和: {sum(nums)}")
print(f"最大值: {max(nums)}")
print(f"最小值: {min(nums)}")
```

</details>
`

---

# 题后回顾

1.变量交换 ：a, b = b, a   # 一行搞定
2.f-string 需要 f"..." 整体包裹。变量用 {变量} 写在字符串内，不需要 `+`
3.else后面有冒号
4.**字符串不可变，方法返回新字符串，需要重新赋值**
