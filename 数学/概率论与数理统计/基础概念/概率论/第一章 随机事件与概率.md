# 第 1 章 随机事件与概率

---

## §1.1 随机事件

### 1.1.1 样本空间

**基本概念**

- **确定性现象**：在一定条件下，必然发生或必然不发生的现象。
- **随机现象**：在一定条件下事先不能确定是否发生的现象。其背后**隐藏着必然规律**（大量重复下呈现规律性）。
- **随机试验**（简称试验，记作 E）：对随机现象的"观察""记录"。

**随机试验的三个特征**

1. **可重复性**：试验可以在相同的条件下重复地进行；
2. **多结果性**：每次的结果不一定相同，试验可能出现的一切结果可事先预知；
3. **不确定性**：但不能事先预知每次试验会出现哪一个结果。

**核心术语**

| 术语 | 定义 | 记号 |
|---|---|---|
| **样本空间** | 随机试验的所有可能结果组成的集合 | Ω |
| **样本点** | 试验的每个可能结果 | ω |
| **基本事件** | 仅由一个样本点组成的集合(不能再分) | {ω} |
| **必然事件** | 每次试验必然发生 | Ω |
| **不可能事件** | 每次试验必不发生 | ∅ |
| **随机事件** | 由某些样本点组成的集合（样本空间的子集），简称事件 | A, B, C, … |

> [!note]
> 📌 样本空间可以是有限集、可列无限集或不可列无限集；可以是数集，也可以是抽象元素的集合，还可以是多维样本点的集合。

### 1.1.2 随机事件的关系与运算

事件是集合，事件的关系与运算按集合论规定。

| 关系/运算 | 定义 | 概率含义 |
|---|---|---|
| **包含** | A ⊂ B | A 发生必然导致 B 发生 |
| **相等** | A = B ⟺ A ⊂ B 且 B ⊂ A | A 与 B 是同一事件 |
| **和（并）** | A ∪ B = {ω \| ω ∈ A 或 ω ∈ B} (**A + B**) | 当且仅当 A 与 B 至少有一个发生，A ∪ B 才发生(A + A = A, A + Ω = Ω) |
| **积（交）** | A ∩ B = {ω \| ω ∈ A 且 ω ∈ B}(简写 **AB**) | 当且仅当 A 与 B 同时发生，AB 才发生(AA = A, A∅ = ∅, AΩ = A) |
| **差** | A − B = {ω \| ω ∈ A 且 ω ∉ B} | A 发生而 B 不发生(A - B = AB̄) |
| **互斥（互不相容）** | AB = ∅ | A 与 B 不能同时发生 |
| **对立（互逆）** | A ∪ B = Ω 且 AB = ∅ | 每次试验 A、B 有且仅有一个发生；A 的对立事件记 Ā |

**运算定律**（与集合运算律相同）

- 交换律、结合律、分配律；
- **德·摩根律（对偶律）⭐**：
  - (A ∪ B) 的补 = Ā ∩ B̄
  - (A ∩ B) 的补 = Ā ∪ B̄

  推广到多个事件：
  - (A₁ ∪ A₂ ∪ … ∪ Aₙ) 的补 = Ā₁ ∩ Ā₂ ∩ … ∩ Āₙ
  - (A₁ ∩ A₂ ∩ … ∩ Aₙ) 的补 = Ā₁ ∪ Ā₂ ∪ … ∪ Āₙ
- A = AB ∪ AB̄
- A - B + B = A + B


> [!tip]
> 📌 **"至少有一个发生"的表述套路**（常考）：
> 设 Aᵢ = "第 i 幢楼房经验收合格"，则
> 
> - "至少有 1 幢合格" = A₁ ∪ A₂ ∪ A₃
> - "恰有 1 幢合格" = A₁Ā₂Ā₃ ∪ Ā₁A₂Ā₃ ∪ Ā₁Ā₂A₃
> - 可推广到无穷：∪Aᵢ、∩Aᵢ 表示可列无穷个事件至少一个发生 / 同时发生。

---

## §1.2 随机事件的概率

### 1.2.1 频率与概率

**定义 1.1**：在相同的条件下，进行 n 次相同的试验，在这 n 次试验中，事件 A 发生的次数 n_A 称为事件 A 发生的**频数**，比值 n_A / n 称为事件 A 发生的**频率**，记为 fₙ(A)。

**频率的基本性质**：

1. 0 ≤ fₙ(A) ≤ 1
2. fₙ(Ω) = 1
3. 若 A₁, …, Aₙ 两两互不相容，则 fₙ(A₁ ∪ … ∪ Aₙ) = fₙ(A₁) + … + fₙ(Aₙ)

**统计规律性**：随着试验次数 n 的增加，fₙ(A) 的波动幅度明显变小，**逐渐稳定于**某一个常数（如抛硬币稳定在 0.5 附近）。这个稳定值就是概率的直观来源。说明随机事件发生的可能性大小，是随机事件本身固有的不随人意志而改变的客观属性，所以随机事件发生的可能性大小是可以度量的，度量值称为**概率**

### 1.2.2 概率的公理化定义

**定义 1.2**：设 E 是随机试验，Ω 是它的样本空间。对 E 的每个事件 A 赋予一个实数 P(A)，如果集合函数 P(·) 满足下列条件，则称 P(A) 为事件 A 的**概率**：

1. **非负性**：对每个事件 A，都有 P(A) ≥ 0；
2. **规范性**：P(Ω) = 1；
3. **可列可加性**：对于可列无穷个两两互斥的事件 A₁, A₂, …，有
   P(A₁ ∪ A₂ ∪ …) = P(A₁) + P(A₂) + …

**概率的运算性质**

| 性质 | 内容 |
|---|---|
| 性质 1 | P(∅) = 0 |
| 性质 2（有限可加性） | 若 A₁, …, Aₙ 两两互斥，则 P(A₁ ∪ … ∪ Aₙ) = ΣP(Aᵢ) |
| **性质 3 ⭐** | 对任意事件 A，P(Ā) = 1 − P(A)（求逆公式） |
| **性质 4 ⭐（加法公式）** | P(A ∪ B) = P(A) + P(B) − P(AB) |
| 推广（三事件） | P(A ∪ B ∪ C) = P(A) + P(B) + P(C) − P(AB) − P(AC) − P(BC) + P(ABC) |
| **减法公式** | P(A − B) = P(A) − P(AB)（因为 A − B = A B̄） |
| **单调性** | 若 A ⊂ B，则 P(A) ≤ P(B)，且 P(B − A) = P(B) − P(A) |
| 性质 6 | 对任意事件 A，有 P(A) ≤ 1 |

**多事件并集的概率公式（容斥原理）**：

\[
P(\bigcup_{i=1}^n A_i) = \sum_{i=1}^n P(A_i) - \sum_{1 \leqslant i < j \leqslant n} P(A_i A_j) + \sum_{1 \leqslant i < j < k \leqslant n} P(A_i A_j A_k) + \dots + (-1)^{n-1} P(A_1 A_2 \dots A_n)
\]

> [!warning]
> 📌 **注意**：P(A) = 0 推不出 A = ∅；P(A) = 1 推不出 A = Ω。概率为 0 的事件也可能发生（几何概型中"投针恰好压在边界上"）。
> 📌 **证明技巧**：证 P(A ∪ B) = P(A) + P(B) − P(AB) 时，把 A ∪ B 拆成 A ∪ (B − AB)，且 A 与 B − AB 互斥——**化难为易，转化到互斥情形**用可加性。

---

## §1.3 古典概型与几何概型

### 1.3.1 古典概型

**两个特点**：
1. 样本空间 Ω **包含有限个样本点**，不妨记 Ω = {ω₁, ω₂, …, ωₙ}
2. **每个样本点在一次试验中以相等的可能性出现**（等可能性），即 P({ω₁}) = … = P({ωₙ}) = 1/n

**计算公式 ⭐**：

> [!NOTE]
> **概率公式：**
>
> $$
> P(A)=\frac{n_A}{n}
> =\frac{\text{事件 }A\text{ 包含的样本点数}}{\text{样本点总数}}
> $$

> [!WARNING]
> 此公式**只适用于有限等可能**的样本空间。

**计数工具——排列组合复习**：

- **排列**（与顺序有关）：
  $$
  A(n,r)=n(n-1)\cdots(n-r+1)=\frac{n!}{(n-r)!}
  $$
  - **全排列**：$n!$

- **组合**（与顺序无关）：
  $$
  C(n,r)=\frac{A(n,r)}{r!}=\frac{n!}{r!(n-r)!}
  $$

- **放回 vs 不放回**；**重复 vs 不重复**；**逐次 vs 一次取出**  
  （注意样本空间构造方式要统一）。

**四大经典模型**：

| 模型 | 情形 | 结果 |
|---|---|---|
| **分房/球入盒** | n 个球随机放入 N 个盒子 | ①指定的 n 个盒子各恰有 1 球：p = n! / Nⁿ；②恰有 n 个盒子各恰有 1 球（每盒至多 1 球）：p = C(N, n)·n! / Nⁿ = A(N, n) / Nⁿ |
| **抽签问题** | a 件次品 b 件正品，不放回取 n 件，恰 k 件次品 | p = C(a, k)·C(b, n−k) / C(a+b, n)（超几何公式） |
| **抽签公平性 ⭐** | 第 k 个人抽到中奖签 | pₖ = a / (a+b)，**先抽和后抽中奖概率一样**（无放回时亦成立） |
| **生日问题** | n 个人中至少两人生日相同 | p = 1 − [365×364×…×(365−n+1)] / 365ⁿ，n = 50 时已达 0.97 |

**小概率事件原理**：概率很小的事件在一次试验中几乎是不发生的（实际推断原理）。反之，若小概率事件在一次试验中发生了，则有理由怀疑假设的正确性。

### 1.3.2 几何概型

**适用情形**：样本点无限，但样本点按"某种"等可能出现（一维长度 / 二维面积 / 三维体积），具有**等可能性**。

**计算公式**：
> [!NOTE]
> **几何概型概率公式：**
>
> $$
> P(A)=\frac{m(A)}{m(\Omega)}
> =\frac{\text{构成事件 }A\text{ 的区域长度（面积、体积）}}{\text{样本空间 }\Omega\text{ 的测度}}
> $$

**典型例题**：
- **两船会面问题 ⭐**：两船在 24 小时内随机到达，先到者等 6 小时，求相遇概率
  P(A) = [24² − (24−6)²] / 24² = 252 / 576 = 0.4375
  （画图：正方形面积减去右上角小正方形）
- **蒲丰投针问题 ⭐**：针与平行线相交的概率 P(A) = 2L / (aπ)。历史上用投针实验的**频率**去估计 π——这就是著名的**蒙特卡罗（Monte Carlo）方法**的思想：设计适当的随机试验，用频率估计概率，从而计算积分等复杂量。

---

## §1.4 条件概率 ⭐

### 条件概率的定义

**实际问题**：事件 A 已发生，在此条件下讨论事件 B 发生的概率。

**定义 1.3**：设 A、B 是两个事件，且 P(A) > 0，称

> [!NOTE]
> **条件概率公式：**
>
> $$
> P(B\mid A)=\frac{P(AB)}{P(A)} \tag{1.7}
> $$
>
> 其中 \(P(A)>0\)

为在事件 A 发生的条件下事件 B 发生的**条件概率**。

> **例（教材引入）**：从 1～10 中任取一数（大前提），已知取到偶数，求是 3 的倍数的概率：
>P(B | A) = n_AB / n_A = 1/5 = P(AB) / P(A)
>（直接法：在缩小的样本空间 A 内用古典概型，P(B | A) = n_AB / n_A——**方法：缩减样本空间**）

**条件概率也是概率**：固定 A 后，P(· | A) 满足概率三公理：

1. 对每个事件 B，P(B | A) ≥ 0；
2. P(Ω | A) = 1；
3. 可列可加性：P(B₁ ∪ B₂ ∪ … | A) = P(B₁ | A) + P(B₂ | A) + …（Bᵢ 两两互斥）。

故概率的一切性质对条件概率都成立，如 P(B̄ | A) = 1 − P(B | A)。

> [!NOTE]
> **条件概率的加法公式：**
>
> $$
> P(B_1 \cup B_2 \mid A) = P(B_1 \mid A) + P(B_2 \mid A) - P(B_1 B_2 \mid A)
> $$

### 乘法公式

由定义 1.3 立即得到：

> [!NOTE]
> **P(AB) = P(A | B)·P(B) = P(B | A)·P(A)**   (1.9, 1.10)

**三事件**：
P(ABC) = P(A | BC)·P(B | C)·P(C)

**n 个事件的推广**（1.12）：
P(A₁A₂…Aₙ) = P(A₁)·P(A₂ | A₁)·P(A₃ | A₁A₂)·…·P(Aₙ | A₁…Aₙ₋₁)

> 📌 **使用条件**：作为条件的事件概率必须大于 0。
> 📌 **典型应用（波利亚罐子模型 ⭐）**：袋中有 a 个红球、b 个白球、c 个黑球，每次取一球，取后**放回并加入同色球 c 个**，取 3 次：
> P(A₁A₂A₃) = [a/(a+b+c)]·[(a+c)/(a+b+2c)]·[(a+2c)/(a+b+3c)]
>（每次条件概率的分母分子同步变化，逐次用乘法公式。）

---

## §1.5 全概率公式与贝叶斯公式 ⭐

### 完备事件组（划分）

**定义 1.4**：设 Ω 为试验 E 的样本空间，B₁, B₂, …, Bₙ 为 E 的一组事件，如果

1. BᵢBⱼ = ∅，i ≠ j，i, j = 1, 2, …, n（两两互斥）；
2. B₁ ∪ B₂ ∪ … ∪ Bₙ = Ω（合起来是必然事件）；

且 P(Bᵢ) > 0，则称 B₁, …, Bₙ 为 Ω 的一个**划分**（完备事件组）。

> ⚠️ **"划分"是全概率公式的前提**——一组事件必须互斥且穷尽所有可能。可推广到可列无穷个：B₁, B₂, … 两两互斥且 ∪Bᵢ = Ω

### 全概率公式

**定理 1.1 ⭐**：设试验 E 的样本空间为 Ω，A 为 E 的事件，B₁, …, Bₙ 为 Ω 的一个划分，且 P(Bᵢ) > 0，则

> [!NOTE]
> **P(A) = Σ P(Bᵢ)·P(A | Bᵢ)**   (1.13)

**直观理解**：若 A 的发生必有某个"原因" Bᵢ 伴随发生，则**每个"原因"发生的概率 × 该"原因"导致 A 发生的条件概率，全部乘积之和即为 P(A)**。
**思想**：把复杂事件 A 分解为若干简单情形，利用概率的可加性"化整为零"。

> **典型题**：仓库中三个厂的产品混放，次品率分别为 0.02、0.01、0.03，份额 15%、80%、5%。任取一件是次品的概率：
> P(A) = 0.15×0.02 + 0.80×0.01 + 0.05×0.03 = 0.0125

### 贝叶斯公式

> [!NOTE]
> **贝叶斯公式：**
>
> $$
> P(B_i \mid A) = \frac{P(B_i)\,P(A \mid B_i)}{\sum_{j=1}^{n} P(B_j)\,P(A \mid B_j)}, \quad i = 1, 2, \dots, n \tag{1.14}
> $$

**两种解读**：
- 分子 P(BᵢA) = P(Bᵢ)P(A | Bᵢ) 是全概率和中的一项；
- **先验概率与后验概率**：P(Bᵢ) 是观察到 A 发生前的"先验概率"；P(Bᵢ | A) 是观察到新信息 A 后，对 Bᵢ 出现可能性的**估计/修正**（后验概率）。**贝叶斯公式是人们根据新的信息修正原有认识的重要方法**，广泛用于医疗诊断、机器检验等。

> **典型题（肝癌普查）**：患病率 P(C) = 0.0004，阳性率 P(A | C) = 0.95，误诊率 P(A | C̄) = 0.10，则
P(C | A) = (0.0004×0.95) / (0.0004×0.95 + 0.9996×0.10) ≈ 0.0038
> 结果很小——尽管检验很准，普查出的阳性者真正患癌的比例仍很低（因先验概率太小），这就是"罕见病检验"陷阱。

> [!tip]
> 📌 **解题套路**：求 P(A)（"由因求果"）→ 全概率；已知 A 发生，找是哪个"原因" Bᵢ（"执果溯因"）→ 贝叶斯。先写出划分，再列表代入。

---

## §1.6 事件的独立性

### 1.6.1 两个事件相互独立

> **引例**：掷硬币两次，A = "第一次正面"，B = "第二次正面"。
> P(B | A) = P(AB) / P(A) = (1/4) / (1/2) = 1/2 = P(B)
> 即 **A 的发生对 B 的发生概率没有影响**。

**定义 1.5 ⭐**：设 A、B 是两个事件，如果满足

> [!note]
> **P(AB) = P(A)·P(B)**
> 则称 A、B **相互独立**，简称 A、B 独立。

> [!tip]
> ⚠️ **三个等价说法**：P(AB) = P(A)P(B) ⟺ P(B | A) = P(B)（P(A) > 0）⟺ P(A | B) = P(A)。
> ⚠️ **独立 ≠ 互斥**：互斥是 AB = ∅（不能同时发生），独立是概率性质（互不影响）。若 P(A) > 0、P(B) > 0，则**独立与互斥不能同时成立**。

**定理 1.3 ⭐**：若事件 A、B 相互独立，则 A 与 B̄、Ā 与 B、Ā 与 B̄ 也相互独立。

> **证明**（思想要会）：
P(ĀB) = P(B) − P(AB) = P(B) − P(A)P(B) = P(B)[1 − P(A)] = P(B)P(Ā)
> **典型应用**：系统可靠性——"至少一个发生"用加法公式或求逆：
P(A ∪ B) = P(A) + P(B) − P(AB) = P(A) + P(B) − P(A)P(B)

### 1.6.2 多个事件相互独立

**三个事件（1.16, 1.17）**：

若P(AB) = P(A)P(B)，P(AC) = P(A)P(C)，P(BC) = P(B)P(C)
且还满足 **P(ABC) = P(A)P(B)P(C)**

则称 A、B、C **相互独立**；若只满足前三条，则称**两两独立**。

> 🔴 **高频考点：两两独立 ≠ 相互独立！** 相互独立 ⟹ 两两独立，反之不然。
> **反例**：4 只球，3 只分别涂红、白、黑，1 只涂三色。A、B、C 分别表示"取到的球涂有红/白/黑色"，则 P(A) = P(B) = P(C) = 1/2，P(AB) = P(AC) = P(BC) = 1/4 = P(A)P(B) 等成立（两两独立 ✓），但 P(ABC) = 1/4 ≠ 1/8 = P(A)P(B)P(C)（不相互独立 ✗）。

**n 个事件**：对任意的 1 < k ≤ n 和任意 1 ≤ i₁ < i₂ < … < i_k ≤ n，都有
P(Aᵢ₁Aᵢ₂…Aᵢₖ) = P(Aᵢ₁)P(Aᵢ₂)…P(Aᵢₖ)
则称 A₁, …, Aₙ 相互独立（共 2ⁿ − n − 1 个等式要验）。

> [!note]
> 📌 若 A₁, …, Aₙ 相互独立，则把其中任意事件换成其对立事件后，所得的 n 个事件**仍相互独立**。

**可靠性理论**（前提：各元件相互独立）：

| 连接方式 | 系统可靠性（每个元件可靠性 pᵢ） |
|---|---|
| **串联**（所有元件都正常，系统才正常） | R_s = p₁p₂…pₙ = Πpᵢ |
| **并联**（至少一个正常，系统就正常） | R_p = 1 − (1−p₁)(1−p₂)…(1−pₙ) = 1 − Π(1−pᵢ) |

### 1.6.3 伯努利（Bernoulli）概型

**两种结果**：许多问题只关心试验中某事件 A 是否发生——A 发生（概率 p）或 Ā 发生（概率 1−p）。这样的试验称为**伯努利试验**。

**n 重伯努利试验**：把伯努利试验 E 独立地重复进行 n 次，称为 n 重伯努利试验。若每次试验中 P(A) = p（0 < p < 1）保持不变，则 n 次试验中 A 恰好发生 k 次的概率为**二项概率公式**：

> [!note]
> **pₙ(k) = C(n, k)·pᵏ·(1−p)ⁿ⁻ᵏ**，k = 0, 1, 2, …, n   (1.18)

> [!note]
> 📌 理解：pᵏ(1−p)ⁿ⁻ᵏ 是**指定的** k 次发生、其余 n−k 次不发生的概率；C(n, k) 是"**哪 k 次发生**"的位置数——先定位置，再算概率。
> 📌 由二项式定理：Σ pₙ(k) = (p + (1−p))ⁿ = 1 ✓

> **典型应用**：某河流每年出现特大洪水的概率为 0.1，且各年独立，则 10 年内至少出现两次特大洪水的概率：
> P = 1 − P(0次) − P(1次) = 1 − 0.9¹⁰ − C(10,1)×0.1×0.9⁹

---

## 📋 本章核心公式速查

| 公式 | 内容 | 适用条件 |
|---|---|---|
| 求逆公式 | P(Ā) = 1 − P(A) | 任意事件 |
| 加法公式 | P(A ∪ B) = P(A) + P(B) − P(AB) | 任意事件 |
| 减法公式 | P(A − B) = P(A) − P(AB) | 任意事件 |
| 古典概型 | P(A) = n_A / n | 有限、等可能 |
| 几何概型 | P(A) = m(A) / m(Ω) | 无限、等可能（按测度） |
| 条件概率 | P(B \| A) = P(AB) / P(A) | P(A) > 0 |
| 乘法公式 | P(AB) = P(B \| A)P(A) | P(A) > 0 |
| 全概率公式 | P(A) = Σ P(Bᵢ)P(A \| Bᵢ) | {Bᵢ} 为划分 |
| 贝叶斯公式 | P(Bᵢ \| A) = P(Bᵢ)P(A \| Bᵢ) / ΣP(Bⱼ)P(A \| Bⱼ) | 同上 |
| 独立性 | P(AB) = P(A)P(B) | 定义式 |
| 二项概率 | pₙ(k) = C(n,k)pᵏ(1−p)ⁿ⁻ᵏ | n 重伯努利试验 |

**易错清单**：
1. 互斥 ≠ 独立（互斥不能同时发生；独立互不影响）；
2. 两两独立 ≠ 相互独立（三个事件要验 4 个等式）；
3. 用全概率/贝叶斯前必须验证 {Bᵢ} 是**划分**（互斥 + 穷尽）；
4. 几何概型中 P(A) = 0 的事件未必不可能发生；
5. "至少有一个"优先考虑对立事件 Ā（求逆公式化繁为简）。

---
