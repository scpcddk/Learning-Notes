# 📝 牛顿迭代法学习笔记

## 一、核心思想（一句话概括）

> **用切线近似曲线，不断逼近方程的根。**  
> 在几何上，牛顿法通过迭代，从初始猜测值开始，沿切线方向逐步“滑”向函数与 x 轴的交点。

---

## 二、数学原理推导

**1. 目标**  
求解方程 $f(x) = 0$ 的根。

**2. 推导过程**  
设当前迭代点为 $x_n$，在 $x_n$ 处做切线（泰勒一阶展开）：
$$f(x) \approx f(x_n) + f'(x_n)(x - x_n)$$
令 $f(x) = 0$（即切线与 x 轴交点），解出 $x$：
$$0 = f(x_n) + f'(x_n)(x - x_n)$$
$$x = x_n - \frac{f(x_n)}{f'(x_n)}$$

**3. 迭代公式**  
将新得到的 $x$ 作为下一次迭代起点 $x_{n+1}$：
$$\boxed{x_{n+1} = x_n - \frac{f(x_n)}{f'(x_n)}}$$

---

## 三、针对“求算术平方根”的应用

**1. 构造函数**  
要求非负整数 $a$ 的平方根，即求 $x = \sqrt{a}$。  
等价于求方程 $f(x) = x^2 - a = 0$ 的正数根。

**2. 求导**  
$$f'(x) = 2x$$

**3. 代入迭代公式**  
$$\begin{aligned}
x_{n+1} &= x_n - \frac{x_n^2 - a}{2x_n} \\\
&= \frac{2x_n^2 - x_n^2 + a}{2x_n} \\\
&= \frac{x_n^2 + a}{2x_n}
\end{aligned}$$

**4. 最终简化式（面试可直接套用）**
$$\boxed{x_{n+1} = \frac{x_n + \frac{a}{x_n}}{2}}$$

---

## 四、算法实现步骤（以 Java 为例）

```java
public int mySqrt(int x) {
    if (x < 2) return x;   // 0 和 1 的平方根就是本身
    long r = x;   // 初始值取 x 本身（或取大一点的值）
    while (r * r > x) {
        r = (r + x / r) / 2;
    }
    return (int) r;
}
```

**模拟过程（求 x = 9 的平方根）**：
- 初始：$r_0 = 9$
- $r_1 = (9 + 9/9) / 2 = 5$
- $r_2 = (5 + 9/5) / 2 = 3.4$
- $r_3 = (3.4 + 9/3.4) / 2 \approx 3.02$
- $r_4 \approx 3.0001$（快速收敛到 3）

---

## 五、关键注意事项（避坑指南）

| 问题 | 解决方案 |
| :--- | :--- |
| **整数溢出** | `r * r` 可能超过 `int` 范围（最大 2^31-1）。<br>**必须用 `long` 类型**存放 `r` 并计算乘积。 |
| **初始值选择** | 取 `r = x` 是最安全简单的策略。<br>（若取 `r = x/2`，需单独处理 `x < 2` 的情况） |
| **停止条件** | 因为是求整数部分，迭代到 `r * r <= x` 时停止，此时 `r` 即为答案。<br>（若求精确小数，则设置容差阈值如 `1e-7`） |
| **处理负数** | 题目保证非负整数，无需考虑负数。 |

---

## 六、收敛速度（为什么它很快？）

- **二次收敛**：每次迭代，有效数字的位数大约翻倍。  
- **示例**：求 $\sqrt{2}$，迭代 4~5 次即可达到双精度浮点数的极限精度。  
- **时间复杂度**：近似 $O(\log \log x)$，远优于二分查找的 $O(\log x)$。

---

## 七、牛顿法 vs. 二分查找（优缺点对比）

| 维度 | 牛顿迭代法 | 二分查找 |
| :--- | :--- | :--- |
| **速度** | 极快（二次收敛） | 较慢（线性收敛） |
| **实现复杂度** | 需要数学推导，需注意溢出 | 逻辑简单，不易出错 |
| **适用性** | 依赖函数可导 | 仅需单调性 |
| **面试推荐** | 展现数学功底，**加分项** | 稳妥保底，**基础解法** |

---

## 八、总结记忆口诀

> **求根切线迭代快，构造方程求导代；**  
> **平方根用和除二，长整防溢莫忘怀。**

只要记住公式 **`r = (r + x / r) / 2`** 和 **防溢出用 long**，遇到平方根面试题就能快速写出最优解。
