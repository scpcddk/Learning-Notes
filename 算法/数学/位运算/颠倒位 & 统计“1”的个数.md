# 📘 位运算算法笔记：颠倒位 & 统计“1”的个数

## 1. 一眼区分（核心对比表）

| 维度 | **颠倒二进制位 (LeetCode 190)** | **位1的个数 / 汉明权重 (LeetCode 191)** |
| :--- | :--- | :--- |
| **目标** | 把 `0` 和 `1` 的**顺序反过来** | 数一数**有几个 `1`** |
| **核心动作** | **拼接（组装）** 新数字 | **消灭（删除）** 原数字里的 `1` |
| **灵魂操作** | `res = (res << 1) \| (n & 1)` | `n &= (n - 1)` （Brian Kernighan） |
| **循环次数** | 固定 **32 次**（int 是 32 位） | 不固定，等于 **`1` 的个数**（最优解） |
| **返回值** | 颠倒后的新整数 | 整数 `1` 的出现次数 |

---

## 2. 题一：颠倒二进制位（190）

### 核心思想（拼图法）

从原数 `n` 的最右边（低位）开始，一个一个把“位”切下来，拼接到结果 `res` 的最右边。每拼一个，`res` 左移腾出空位，`n` 右移丢掉旧位。

### 标准解法（必背模板）

```java
public int reverseBits(int n) {
    int res = 0;
    for (int i = 0; i < 32; i++) {
        // 1. 左移腾位置  |  2. 取出 n 的最低位拼上去
        res = (res << 1) | (n & 1);
        n >>>= 1; // 无符号右移，处理下一位
    }
    return res;
}
```

**手算模拟（4位示例）**：  
`n=1011` → 取尾数 `1`→`res=0001`，取尾数 `1`→`res=0011`，取尾数 `0`→`res=0110`，取尾数 `1`→`res=1101`。完成！

---

## 3. 题二：位1的个数（191）

### 解法一：普通扫描（通用，易理解）

```java
public int hammingWeight(int n) {
    int count = 0;
    for (int i = 0; i < 32; i++) {
        count += (n & 1); // 取最低位，累加
        n >>>= 1;
    }
    return count;
}
```

### 解法二：Brian Kernighan 算法（✨ 重点掌握，最优美）

**公式**：`n = n & (n - 1)`  
**效果**：直接**消除** `n` 的二进制中**最右边的那个 `1`**。

- **为什么能消除？（图解记忆法）**

- 以 `n = 101000`（40）为例：
  - `n - 1 = 100111`（最右边的 `1` 变 `0`，右边所有 `0` 变 `1`）
  - `n & (n-1) = 101000 & 100111 = 100000`（完美删除最右 `1`）
- 每执行一次，就砍掉一个 `1`，执行次数 = `1` 的个数。

```java
public int hammingWeight(int n) {
    int count = 0;
    while (n != 0) {
        n &= (n - 1); // 砍掉最右边的 1
        count++;
    }
    return count;
}
```

---

## 4. ⚠️ 最高频的“坑”：为什么必须用 `>>>` 而不是 `>>`？

- `>>` 是**有符号右移**：左边补**符号位**（负数补 `1`，正数补 `0`）。
  - 如果 `n` 是负数（如 `-1`，二进制全 `1`），`n >> 1` 永远补 `1`，**`n` 永远变不成 0**，导致**死循环**。
- `>>>` 是**无符号右移**：左边**永远补 `0`**。
  - 负数经过 32 次 `>>>` 后必定变成 0，循环正常结束。

**记忆口诀**：**位运算处理负数，无符号右移保平安！**

---

## 5. 两道题的“错题本”警示

1. **忘记固定循环次数**：190 题必须循环满 32 次，不能写成 `while(n != 0)`，因为原数高位补的 0 也要颠倒成低位，不能丢掉！
2. **括号问题**：位运算优先级低于加减法，`res << 1 | n & 1` 在 Java 中不报错但逻辑易错，**强烈建议加括号** `(res << 1) | (n & 1)`。
3. **类型溢出**：190 题如果中间结果超过 int 范围？不用管，Java 的 int 是 32 位，自动截断，正好符合题目要求。

---

## 6. 一句话面试秒答（装X必备）

> **问：如何数二进制中的 1？**  
> **答**：用 Brian Kernighan 算法，`n &= (n-1)` 每次消灭最右边的 `1`，消灭几次就有几个 `1`，比遍历 32 位更优雅。

> **问：如何颠倒二进制位？**  
> **答**：把原数的低位逐个取出，通过 `左移` 拼接到结果数的低位，循环 32 次即可，注意用无符号右移。

---

只要把这份笔记里的 **“拼图法”** 和 **“砍 1 法”** 区分开，遇到这两道题绝对是秒杀。复习时拿着笔在纸上画一遍 `101000 & 100111` 的过程，Brian Kernighan 算法就永远忘不掉了！加油 💪
